# Where all your movies and shows disappear into the deep… and instantly resurface
# in Plex like nothing ever happened.
name: new-providence-island

services:
  bazarr:
    image: lscr.io/linuxserver/bazarr:latest
    container_name: bazarr
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - bazarr:/config
      - /mnt/prime-ds/movies:/movies
      - /mnt/prime-ds/series:/series
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bazarr.rule=Host(`bazarr.${DOMAIN}`)"
      - "traefik.http.routers.bazarr.entrypoints=websecure"
      - "traefik.http.routers.bazarr.tls.certresolver=letsencrypt"
      - "traefik.http.services.bazarr.loadbalancer.server.port=6767"

  cleanuparr:
    image: ghcr.io/cleanuparr/cleanuparr:latest
    container_name: cleanuparr
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - cleanuparr:/config
      - /mnt/downloads:/downloads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://cleanuparr:11011/health"]
      interval: 30s
      timeout: 10s
      start_period: 30s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.cleanuparr.rule=Host(`cleanuparr.${DOMAIN}`)"
      - "traefik.http.routers.cleanuparr.entrypoints=websecure"
      - "traefik.http.routers.cleanuparr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.cleanuparr.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.routers.cleanuparr.middlewares=authentik@file"
      - "traefik.http.services.cleanuparr.loadbalancer.server.port=11011"

  kometa:
    image: lscr.io/linuxserver/kometa:latest
    container_name: kometa
    networks:
      - dirty-water
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}    
    volumes:
      - kometa:/config
    restart: unless-stopped

  lidarr:
    image: lscr.io/linuxserver/lidarr:latest
    container_name: lidarr
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - lidarr:/config
      - /mnt/prime-ds/music:/music
      - /mnt/downloads:/downloads
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.lidarr.rule=Host(`lidarr.${DOMAIN}`)"
      - "traefik.http.routers.lidarr.entrypoints=websecure"
      - "traefik.http.routers.lidarr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.lidarr.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.lidarr.loadbalancer.server.port=8686"

  plex:
    image: 'lscr.io/linuxserver/plex:latest'
    container_name: plex
    networks:
      dirty-water:
      macvlan:
        ipv4_address: ${MACVLAN_STATIC_IP_PLEX}
      web:
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - VERSION=latest
      - TZ=${TZ}
      - plex_HW_ACCEL=true
      - GIDLIST=44,993
      - LIBVA_DRIVER_NAME=iHD
      - LIBVA_DRIVERS_PATH=/usr/lib/x86_64-linux-gnu/dri
    devices:
      - '/dev/dri:/dev/dri'    
    device_cgroup_rules:
      - 'c 226:* rmw'    
    group_add:
      - 'video'  # "44"
      - 'render' # "993"
    volumes:
      - 'plex:/config'
      - '/mnt/prime-ds/movies:/volume1/movies'
      - '/mnt/prime-ds/music:/volume1/music'
      - '/mnt/prime-ds/series:/volume1/series'
      - '/mnt/prime-ds/movies:/movies'
      - '/mnt/prime-ds/music:/music'
      - '/mnt/prime-ds/series:/series'
      - '/usr/lib/x86_64-linux-gnu/dri:/usr/lib/x86_64-linux-gnu/dri:ro'
    restart: unless-stopped 
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.plex.rule=Host(`plex.${DOMAIN}`)"
      - "traefik.http.routers.plex.entrypoints=websecure"
      - "traefik.http.routers.plex.tls.certresolver=letsencrypt"
      - "traefik.http.routers.plex.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.plex.loadbalancer.server.port=32400"

  plex-auto-languages:
    image: remirigal/plex-auto-languages:latest
    container_name: plex-auto-languages
    environment:
      - TZ=${TZ}
    networks:
      dirty-water:
    volumes:      
      - plexautolanguages:/config
    restart: unless-stopped

  posterizarr:
    image: ghcr.io/fscorrupt/posterizarr:latest
    container_name: posterizarr
    environment:
      - TZ=${TZ}
      - TERM=xterm
      - RUN_TIME=disabled
    restart: unless-stopped
    user: ${PUID}:${PGID}
    networks:
      - web
    volumes:
      - posterizarr:/config:rw
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.posterizarr.rule=Host(`posterizarr.${DOMAIN}`)"
      - "traefik.http.routers.posterizarr.entrypoints=websecure"
      - "traefik.http.routers.posterizarr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.posterizarr.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.posterizarr.loadbalancer.server.port=8000"

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    container_name: prowlarr
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - prowlarr:/config
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prowlarr.rule=Host(`prowlarr.${DOMAIN}`)"
      - "traefik.http.routers.prowlarr.entrypoints=websecure"
      - "traefik.http.routers.prowlarr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.prowlarr.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.prowlarr.loadbalancer.server.port=9696"

  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - radarr:/config
      - /mnt/prime-ds/movies:/movies
      - /mnt/downloads:/downloads
      - /mnt/downloads/transmission/completed:/data/completed
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.radarr.rule=Host(`radarr.${DOMAIN}`)"
      - "traefik.http.routers.radarr.entrypoints=websecure"
      - "traefik.http.routers.radarr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.radarr.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.radarr.loadbalancer.server.port=7878"
  
  sabnzbd:
    image: lscr.io/linuxserver/sabnzbd:latest
    container_name: sabnzbd
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - sabnzbd:/config
      - /mnt/downloads:/downloads
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sabnzbd.rule=Host(`sabnzbd.${DOMAIN}`)"
      - "traefik.http.routers.sabnzbd.entrypoints=websecure"
      - "traefik.http.routers.sabnzbd.tls.certresolver=letsencrypt"
      - "traefik.http.routers.sabnzbd.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.sabnzbd.loadbalancer.server.port=3389"
  seerr:
    container_name: seerr
    image: ghcr.io/seerr-team/seerr:latest
    networks:
      - web
    restart: unless-stopped
    volumes:
      - seerr:/app/config
    environment:
      - TZ=${TZ}
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:5055/api/v1/status || exit 1
      start_period: 20s
      timeout: 3s
      interval: 15s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.seerr.rule=Host(`seerr.${DOMAIN}`)"
      - "traefik.http.routers.seerr.entrypoints=websecure"
      - "traefik.http.routers.seerr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.seerr.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.seerr.loadbalancer.server.port=5055"

  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - sonarr:/config
      - /mnt/downloads:/downloads
      - /mnt/downloads/transmission/completed:/data/completed
      - /mnt/prime-ds/series:/tv
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sonarr.rule=Host(`sonarr.${DOMAIN}`)"
      - "traefik.http.routers.sonarr.entrypoints=websecure"
      - "traefik.http.routers.sonarr.tls.certresolver=letsencrypt"
      - "traefik.http.routers.sonarr.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.sonarr.loadbalancer.server.port=8989"

  tautulli:
    image: lscr.io/linuxserver/tautulli:latest
    container_name: tautulli
    networks:
      - web
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - tautulli:/config
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tautulli.rule=Host(`tautulli.${DOMAIN}`)"
      - "traefik.http.routers.tautulli.entrypoints=websecure"
      - "traefik.http.routers.tautulli.tls.certresolver=letsencrypt"
      - "traefik.http.routers.tautulli.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.services.tautulli.loadbalancer.server.port=8181"

  transmission:
    image: haugene/transmission-openvpn
    container_name: transmission
    networks:
      - web
    cap_add:
      - NET_ADMIN
    volumes:
      - transmission:/config
      - /mnt/downloads/transmission:/data
      - /mnt/downloads/transmission/watch:/watch
    env_file:
      - transmission.env
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - NORDVPN_COUNTRY=${NORDVPN_COUNTRY}
      - OPENVPN_CONFIG=${NORDVPN_COUNTRY}
      - OPENVPN_USERNAME=${OPENVPN_USERNAME}
      - OPENVPN_PASSWORD=${OPENVPN_PASSWORD}
    logging:
      driver: json-file
      options:
        max-size: 11m
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.transmission.rule=Host(`transmission.${DOMAIN}`)"
      - "traefik.http.routers.transmission.entrypoints=websecure"
      - "traefik.http.routers.transmission.tls.certresolver=letsencrypt"
      - "traefik.http.routers.transmission.priority=${TRAEFIK_PRIORITY}"
      - "traefik.http.routers.transmission.middlewares=authentik@file"
      - "traefik.http.services.transmission.loadbalancer.server.port=9091"

networks:
  dirty-water:
    external: true
  web:
    external: true
  macvlan:
    external: true
  backend:
    external: true

volumes:
  bazarr:
    external: true
  cleanuparr:
    external: true
  huntarr:
    external: true
  kometa:
    external: true
  lidarr:
    external: true
  seerr:
    external: true
  plex:
    external: true
  plexautolanguages:
    external: true
  posterizarr:
    external: true
  prowlarr:
    external: true
  radarr:
    external: true
  sabnzbd:
    external: true
  sonarr:
    external: true
  tautulli:
    external: true
  transmission:
    external: true