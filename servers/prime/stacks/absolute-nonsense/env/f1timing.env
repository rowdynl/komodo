PORT=8000
DATA_DIR=/data
STORAGE_MODE=local
OPENROUTER_API_KEY=${F1REPLAY_OPENROUTER_API_KEY}

# Optional — authentication
# AUTH_ENABLED=true
# AUTH_PASSPHRASE=${F1REPLAY_AUTH_PASSPHRASE}

# Only needed for pre-compute script (not required at runtime)
FASTF1_CACHE_DIR=/data/fastf1-cache